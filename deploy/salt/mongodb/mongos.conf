{% set config_svrs = [] %}

{% for server, addrs in salt['mine.get']('roles:mongodb-cfg', 'network.ip_addrs', expr_form='grain').items() %}
{% set m =  config_svrs.append(addrs[0]+':27017') %}
{% endfor %}

logpath={{ pillar['mongodb']['settings']['log_path'] }}/mongos.log
configdb={{ config_svrs|join(",") }}

